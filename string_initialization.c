#include <stdio.h>
int main()
{
    char s[7] = {'S', 'H', 'A', 'K', 'I', 'L'}; // here 7 because we need to give space for '\0' or null

    char str[20] = "shahriar shakil";
    printf("%s", str);

    return 0;
}